# 独立 OA 任务工作台

独立任务使用 `bpm_oa_task` 真源，不复用 CRM 对象任务。任务包含创建人、负责人、参与人、优先级、截止时间、提醒配置和可选业务引用。

支持创建、更新、删除、查询、开始和完成；访问范围由创建人、负责人或参与人决定。状态为待开始、进行中和已完成，已完成任务不可编辑或删除。截止前提醒由幂等 Job 发送站内信，失败可重试。

入口挂在协作工作区，权限为 `bpm:oa-task:query/create/update/delete`。
