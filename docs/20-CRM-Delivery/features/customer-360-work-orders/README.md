# 客户 360 工单记录（GAP-CUS-003）

客户详情新增“工单记录”Tab，展示工单编号、标题、类型、状态、处理人、创建时间，支持
分页和打开完整处理轨迹。

该 Tab 复用 `/crm/work-order/page?customerId=...`，不新增绕过权限的客户聚合接口：

- 默认只展示当前用户创建或处理的该客户工单；
- 拥有 `crm:work-order:query-all` 时展示租户内该客户全部工单；
- 详情继续由工单接口做相同对象范围校验。
