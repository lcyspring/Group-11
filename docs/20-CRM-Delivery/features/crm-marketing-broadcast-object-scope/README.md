# CRM-FEATURE-056：群发任务与收件人对象范围

普通群发查询用户只能读取本人创建的任务；CRM 管理员、审核角色和发送角色因承担租户级处理职责，
可读取全租户任务。列表在 SQL 层按创建人收敛，详情、目标对象 ID 和收件人手机号/邮箱统一复用
同一 Service 门禁，不能通过切换接口绕过。

真实验收使用两个临时用户证明普通用户列表 1 条、审核人列表 2 条，以及他人详情和收件人均拒绝，
验收后临时用户、角色、任务、收件人和授权残留为 0。
