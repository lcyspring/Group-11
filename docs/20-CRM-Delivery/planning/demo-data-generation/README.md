# 可重复 CRM 广覆盖演示数据生成与替换

制定日期：2026-07-18。实施分支：`develop`。

## 目标与边界

生成关系可靠、适合 CRM 统计、财务、营销、OA 和工单联合验收的演示数据。生成器只输出 SQL、manifest
和 checksum，不连接数据库，也不调用部署脚本；数据替换仅由 `deploy.sh` 消费已生成 manifest。

生成与部署入口均已统一读取显式 KDL。生成器只生成数据集；部署是否保持、插入或替换数据，仍只由
运行 KDL 的 `mysql.dataset_mode` 决定。

## 当前规模

| 对象 | 数量 |
|---|---:|
| 客户 / 联系人 / 线索 / 跟进 | 120 / 120 / 120 / 300 |
| 商机 / 阶段 / 产品 / 合同 | 180 / 5 / 80 / 72 |
| 回款计划 / 回款 / 发票 / 报销 / 退款 | 144 / 90 / 60 / 60 / 30 |
| 营销活动 / 客户关怀 | 36 / 120 |
| OA 日程 / OA 任务 / CRM 工单 | 100 / 150 / 160 |

## 可登录演示组织

生成器创建 8 个启用账号和 5 类权限角色。`demo_user_password_source: owner` 显式要求账号密码哈希从
`owner_user_id` 指定的基准账号复制，因此登录密码与该基准账号当前密码一致；本机当前批次使用管理员
演示密码 `admin123`。用户名只包含字母和数字：

| 用户名 | 昵称 | 角色与数据关联 |
|---|---|---|
| `d20260718user01` | 演示主管·林然 | 销售主管、CRM 管理员；客户/商机/合同/回款 |
| `d20260718user02` | 演示销售·周宁 | 销售专员、仅本人范围；客户/商机/合同/回款 |
| `d20260718user03` | 演示销售·陈曦 | 销售专员、仅本人范围；客户/商机/合同/回款 |
| `d20260718user04` | 演示销售·顾言 | 销售专员、仅本人范围；客户/商机/合同/回款 |
| `d20260718user05` | 演示财务·苏禾 | 财务专员；发票、报销、退款 |
| `d20260718user06` | 演示财务·方晴 | 财务专员、财务审批人；发票、报销、退款 |
| `d20260718user07` | 演示客服·江遥 | 客服专员；80 张工单 |
| `d20260718user08` | 演示客户成功·夏语 | 营销与客户成功；营销活动、80 张工单 |

角色菜单由权限码选择并递归补齐父菜单。销售专员不拥有开票权限，财务拥有开票权限，客服拥有工单处理
权限，营销用户拥有活动管理权限；加载后 SQL 会验证这些权限没有退化为同一角色。

时间范围为 2025-07-01 至 2026-07-18。固定 seed 和相同配置会生成相同业务键、金额和状态分布；
数据库自增 ID 不作为确定性输出的一部分。

## 统计覆盖设计

- 合同少于商机，保留开放、赢单、输单和无效商机；
- 批次拥有独立五阶段流程，每个阶段必须至少有一条开放商机；
- 开放商机有预计成交时间，赢单有实际结单时间；
- 客户覆盖地区、行业、来源、等级和生命周期维度；
- 回款计划同时覆盖已回款、未回款逾期和未回款未来计划；
- 发票、回款、报销、退款、营销关怀和工单均覆盖多个合法状态；
- 审批完成记录只标记为导入历史，不伪造 Flowable 实例；可编辑草稿保留真实前端操作能力。

## 使用

```bash
bash podman/operations/database/generate-demo-dataset.sh \
  podman/config/generate-demo-dataset.example.kdl
```

将 `operation.mode` 设为 `check` 时只校验配置；设为 `generate` 时写入被 Git 忽略的
`database/generated/crm-demo-v2/`。运行库替换时，本机配置临时使用：

```kdl
mysql {
  dataset "crm-demo-v2"
  dataset_manifest "../../database/generated/crm-demo-v2/crm-demo-v2.manifest"
  dataset_mode "replace"
}
```

执行 `bash podman/deploy.sh <runtime-config>` 后必须把 `dataset_mode` 恢复为 `preserve`。

## 可执行质量门禁

manifest 最后一项为 `04-validate.sql`，会在服务启动前校验账号可登录约束、角色权限隔离、多人负责人、
数量、阶段非空、四类结单结果、预测与实际、客户画像、逾期与未来计划、多状态覆盖和关键引用。任一断言
失败都会中止部署，不允许启动一个看似有数据、实际统计退化的环境。
