# 首联系人测试结果

执行日期：2026-07-13

## 自动化与构建

| 检查 | 结果 |
|---|---|
| `CrmContactServiceImplTest` | 16/16 通过 |
| CRM `Crm*Test` 全量回归 | 33/33 通过 |
| 前端改动 ESLint | 通过 |
| Web `build:prod` | 通过 |
| Server 生产 JAR | 通过 |
| Rootless Podman Server/Web/Mall | 健康 |

## Podman 真实场景

| 验证 | 实际结果 | 状态 |
|---|---|---|
| 首个联系人自动设置 | 请求 `false`，读取为 `primaryContact=true` | 通过 |
| 第二联系人默认不切换 | 首联系人仍为第一个 | 通过 |
| 显式切换 | 原联系人 `false`，新联系人 `true` | 通过 |
| 直接取消 | 返回 `1020003004` | 通过 |
| 删除首联系人 | 返回 `1020003005` | 通过 |
| 跨客户移动 | 返回 `1020003006` | 通过 |
| 并发切换 | 两个 PUT 均成功，最终首联系人计数 1 | 通过 |
| 客户响应 | 返回首联系人姓名和手机 `18000000000` | 通过 |
| 操作日志 | 实际产生 6 条 CRM 联系人生命周期日志 | 通过 |

真实验证合计：9/9。首次并发验证发现 `CRM-CORE-BUG-005`，修复后复跑通过。

## 锁等待当前读场景

| 验证 | 实际结果 | 状态 |
|---|---|---|
| 旧表单更新 | 普通联系人在等待客户锁期间被设为首联系人；旧 `primaryContact=false` 请求返回 `1020003004`，最终首联系人仍为该联系人 | 通过 |
| 旧快照删除 | 普通联系人在等待客户锁期间被设为首联系人；删除返回 `1020003005`，联系人仍存在 | 通过 |

锁等待并发验证合计：2/2，覆盖 `CRM-CORE-BUG-007`。临时脚本：`/tmp/crm-contact-current-read-integration.sh`，不提交 Git。

首联系人脚本首次回归因多个联系人复用同一手机号触发 `1020003008`；修正测试号码后 9/9 通过，详见 `TEST-BUG-002`。

## 迁移与清理

- `new-crm-contact-primary.sql` 连续执行两次成功。
- 历史 6 个有有效联系人的客户均恰好一个首联系人。
- 最终清理核对：临时客户 0、联系人 0、操作日志 0、权限 0；同时清理了更早测试遗留的 4 条已逻辑删除客户、5 条已逻辑删除权限和 13 条日志。
- MySQL 客户端清理必须显式使用 `--default-character-set=utf8mb4`，详见 `TEST-BUG-001`。
