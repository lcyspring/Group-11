# 管理端遗留媒体 ORB 测试计划

- 只屏蔽 YAML 明确列出的退休媒体源；
- 同源、其他外部源、相对路径和非法 URL 保持不变；
- 递归处理 API 与缓存中的对象、数组、重复引用和循环引用；
- 登录用户历史头像归一化为空后使用现有默认头像；
- Axios 成功响应和本地用户缓存均经过同一规范化入口；
- Ubuntu 26.04 测试行/分支/方法覆盖率均达到 90%；
- production bundle 注入显式遗留源且 8081 favicon 返回 200。
