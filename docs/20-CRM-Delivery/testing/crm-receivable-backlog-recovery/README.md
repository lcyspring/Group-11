# 回款待办恢复专项测试

## 测试范围

- 运行库幂等迁移；
- 待审核回款接口；
- 回款提醒接口；
- 百度统计默认关闭；
- Ubuntu 26.04 Web 构建和 Web 容器热替换。

## 结果

| 检查项 | 结果 |
| --- | --- |
| 迁移前字段检查 | `source_business_id = 0`，复现结构不一致 |
| 幂等迁移 | 通过，2 个字段、2 个索引存在 |
| 待审核回款 | `code=0`，`total=1` |
| 回款提醒 | `code=0`，`total=3` |
| Ubuntu 26.04 Web 构建 | 通过 |
| 构建产物第三方统计扫描 | 0 处命中 |
| 部署容器第三方统计扫描 | 0 处命中 |
| Web / Server 健康检查 | 200 / 200 |

## 覆盖率

本批次修复位于 Shell/YAML 构建部署层和数据库迁移层，不进入 JaCoCo 指标；以真实
数据库回归、静态产物扫描和 HTTP 验收作为覆盖证据。后续 CRM Java 批次仍继续记录
JaCoCo 指令、分支、行、方法覆盖率。
