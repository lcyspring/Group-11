# 业绩目标运行迁移测试计划

1. 全部 `database/migrations/new-crm-*.sql` 必须进入兼容迁移 manifest。
2. `new-crm-performance-target.sql` 必须可重复执行且不重复生成权限或多语言数据。
3. 运行数据库必须存在目标表、2 个维护权限和 6 条三语权限记录。
4. 目标列表 API 必须返回数组；目标完成度 API 必须返回 12 个月及字符串精度金额。
5. 前端请求失败必须显示错误和重试动作，不能显示为“未配置目标”。
6. 验收命令只能接收一个显式 YAML 配置文件路径。
