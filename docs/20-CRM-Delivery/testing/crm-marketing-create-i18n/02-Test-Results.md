# CRM 营销新增按钮国际化测试结果

日期：2026-07-17。分支：`develop`。

- CRM 营销页面 4/4 使用 `action.create`；
- 缺失键 `common.create` 精确残留 0；
- zh-CN、en、ar 动作词典 3/3 定义 `action.create`；
- Node 专项测试 1/1、ESLint 通过；
- Ubuntu 26.04 Vite production build 成功；
- Web 运行镜像热替换成功，8081 返回 200，Server `UP`，Mall 8082 返回 200。
