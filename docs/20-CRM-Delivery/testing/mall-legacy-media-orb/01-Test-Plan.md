# 测试计划

1. 单元测试本地静态映射、历史上传回退、活跃远程源保留和相似域名隔离；
2. 单元测试嵌套/重复/循环 API 载荷，覆盖分类数据绕过组件工具的场景；
3. 在 Ubuntu 26.04 HBuilderX 容器内运行测试并构建 H5；
4. 用 `rebuild-mall` 替换运行容器；
5. 对用户报告中的资源逐一检查 HTTP 状态与图片 MIME；
6. 检查 H5 入口存在本地 favicon；
7. 保持 Server、Web 及基础设施容器不中断。
