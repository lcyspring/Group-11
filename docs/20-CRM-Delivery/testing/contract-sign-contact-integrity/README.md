# 合同签约联系人归属完整性测试

执行日期：2026-07-14。关联缺陷：CRM-CONTRACT-BUG-004。

## 范围

- 合同指定的签约联系人存在；
- 签约联系人的 `customerId` 必须等于合同 `customerId`；
- 创建、修改和商机转合同共用服务端关系校验；
- 前端联系人下拉过滤不作为安全边界。

## 结果

Ubuntu 26.04 容器 CRM 全量测试 193/193，失败、错误、跳过均为 0。新增用例
`createContractRejectsSignContactFromAnotherCustomer` 验证业务码 `1020000012`，并确认失败请求
不会调用合同 Mapper 写入。

JaCoCo clean 基线：指令 38.29%（8742/22830）、分支 35.98%（458/1273）、
行 36.12%（1624/4496）、方法 24.53%（297/1211）。
