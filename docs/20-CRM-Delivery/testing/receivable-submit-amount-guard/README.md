# 回款提交金额守恒测试

- 缺陷：`CRM-CORE-BUG-004`
- 自动化：`CrmReceivableServiceImplTest` 1/1 通过。
- 关键断言：先锁合同、锁后复读草稿、额度不足不调用 BPM。
- Podman：合同 100、审批中 60、草稿 50，提交返回剩余 40；草稿状态不变。
- 清理：客户、合同、回款残留均为 0。
- 覆盖率：本轮在结构化要求提出前未单独采集 JaCoCo；下次核心回归需纳入同一覆盖率命令，不以 1/1 通过代替代码覆盖率。
