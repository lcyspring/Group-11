# CRM 可观测诊断包测试计划

日期：2026-07-17。分支：`develop`。

1. 检查 Bash 语法和单 YAML 参数契约；
2. 在七个服务容器运行时采集健康、日志、资源与 MySQL 指标；
3. 验证正常阈值返回 PASS，摘要和 `.tar.gz` 同时存在；
4. 不停服务地把磁盘最低可用阈值临时设为 100%，验证当前 72% 触发 FAIL；
5. 验证失败项可定位、摘要目录不被删除、所有容器无重启；
6. 检查原始日志和本机凭据均被 Git ignore。
