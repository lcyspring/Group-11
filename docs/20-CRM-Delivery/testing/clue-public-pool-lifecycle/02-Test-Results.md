# 公共线索完整状态机测试结果

- 日期：2026-07-15
- 环境：Ubuntu 26.04 构建容器、rootless Podman、MySQL 8.0
- 状态：通过

| 检查项 | 结果 |
|---|---|
| CRM 全量自动化 | 324/324，失败 0、错误 0、跳过 0 |
| 公共线索服务专项 | 11/11 |
| 原线索服务专项 | 4/4 |
| 公共/垃圾池权限切面专项 | 4/4 |
| YAML 配置专项 | 4/4 |
| 退回原因请求校验 | 2/2 |
| 前端专项 ESLint | 0 warning、0 error |
| Web production build | Ubuntu 26.04，成功 |
| Server production build | Ubuntu 26.04，37 个 reactor 模块成功 |
| MySQL 迁移幂等 | 完整启动一次、`rebuild-server` 重复执行三次，均成功 |
| 创建未分配线索 | `code=0`，公共分页和详情返回 `poolStatus=1` |
| 领取和手工退回 | OWNER/PUBLIC 状态、前负责人和累计入池次数正确 |
| 手工退回原因快照 | 公共分页和详情原样回显 26 字实际原因 |
| 普通/公共列表隔离 | 退回前普通分页 1 条；退回后公共分页 1 条、普通分页 0 条 |
| 重复领取冷却 | 返回 `1020001006` 和 30 天配置值 |
| 每日领取额度 | 计数达到 10 后返回 `1020001005`，线索仍为 PUBLIC |
| 主管分配 | `code=0`，目标用户领取计数前后均为 0 |
| 同一线索并发领取 | 一次 `code=0`、一次 `1020001003`，SELF_CLAIM 事件仅 1 条 |
| 不同线索并发容量 | 99 + 2 并发时一次成功、一次 `1020001007`，最终严格为 100 |
| 批量操作审计 | 分配成功，操作日志 `biz_id=411`，无列表解析异常 |
| 普通更新负责人篡改 | 名称更新成功，负责人仍为原用户 1 |
| 已转换线索退回 | 返回 `1020001002`，保持转换终态 |
| 公共对象权限 | SELF 范围只读角色读取 PUBLIC 成功，读取他人在管线索返回 `1020007001` |
| 测试数据清理 | 线索、客户、联系人、归属记录、权限、领取计数、测试日志、临时用户和角色均为 0 |
| 服务健康 | 8080 `UP`，8081/8082 HTTP 200；最终重启后无新增 ERROR |

容量并发验收先复现了可重复读快照穿透，再复现了 `INSERT IGNORE` 与 `FOR UPDATE` 的锁升级
死锁；两项均修复后重新构建、部署和并发验收，最终请求返回稳定业务码而不是 500。
