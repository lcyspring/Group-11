# CRM 对话框异常边界测试计划

1. `cancel`、`close` 被识别为用户取消并返回空结果；
2. `confirm` 正常透传；
3. API、网络及未知异常原样抛出；
4. 递归扫描全部 CRM Vue 文件，禁止空 `catch`；
5. 对 16 个改动页面和工具执行 ESLint 零警告门禁；
6. 组合执行垃圾池、回款引用完整性和统计回归；
7. 在 Ubuntu 26.04 容器执行生产构建与语言包完整性校验。
