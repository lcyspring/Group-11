# 孤立审批恢复与详情可靠性测试计划

1. 核对审批中业务记录的流程 ID 是否存在于 `ACT_HI_PROCINST`；
2. 验证五类 CRM 审批聚合均进入幂等修复范围；
3. 验证修复前写入不可变审计快照，修复后状态为已取消且可重提；
4. 连续执行两次迁移，验证审计记录和业务状态不重复变化；
5. 验证详情加载严格先详情后模型，空 ID 和两个请求失败均被捕获；
6. 在 Ubuntu 26.04 容器执行专项、ESLint 和 Web production build。
