# 测试计划：固定种子演示数据

1. `check` 校验名称、seed、日期、规模上限、输出目录和确认开关，不写文件；
2. `generate` 生成 insert、cleanup、manifest、SHA-256，且不访问数据库；
3. 在临时数据库复制六张所需表结构，执行 20 客户、30 商机、40 工单的小规模 SQL；
4. 核对六类数量、客户/商机/工单孤儿引用、客户生命周期与工单状态不变量；
5. 执行 cleanup，六表相关批次总残留必须为 0；
6. 删除临时数据库，不触碰当前业务库。
