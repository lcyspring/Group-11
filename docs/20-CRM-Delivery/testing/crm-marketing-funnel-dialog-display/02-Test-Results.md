# CRM 群发与漏斗弹窗显示测试结果

- 日期：2026-07-18
- 结果：通过

| 测试项 | 结果 |
|---|---:|
| 前端群发、漏斗与响应式弹窗专项 | 18/18 通过 |
| 收件人响应组装器专项 | 1/1 通过 |
| CRM 后端全量 | 537/537 通过 |
| 最终失败 / 错误 / 跳过 | 0 / 0 / 0 |

第一次后端全量测试发现客户级收件人的 `contactId` 为空时，不可空键 Map 的 `get(null)` 会抛出
异常；增加显式空 ID 分支后重新执行全量，最终通过。该用例已经保留，防止问题回归。

补充验证普通滚动弹窗使用 `max-height` 而不是固定 `height`：少量收件人结果会按内容收缩，只有
全屏模式才占满可用滚动高度。专项 ESLint 为 0 warning，行、分支和函数覆盖率均为 100%。
