# 销售漏斗同级状态隔离测试结果

- 日期：2026-07-18
- 结果：通过

## 自动化结果

| 测试项 | 结果 |
|---|---:|
| `CrmStatisticsFunnelServiceImplTest` | 6/6 通过 |
| `CrmBusinessMapperTest` | 1/1 通过 |
| CRM 模块全量测试 | 536/536 通过 |
| 失败 | 0 |
| 错误 | 0 |
| 跳过 | 0 |

## 断言结果

- 独立阶段输入 `10 / 4 / 1`，输出仍为 `10 / 4 / 1`；
- 独立阶段金额没有跨状态累加；
- 明细查询包含精确 `status_id =`；
- 明细查询不包含 `>=` 排序范围；
- 终态赢单、输单、无效仍独立统计。

## 运行态验证

替换 Server 后调用真实状态组接口，结果如下：

| 状态 | 汇总数量 | 明细总数 |
|---|---:|---:|
| 吃饭 | 2 | 2 |
| 睡觉 | 0 | 0 |
| 阿豆豆 | 2 | 2 |

“睡觉”没有因“阿豆豆”的当前存量而被累计，三个详情列表的商机 ID 也没有交叉。

首次运行曾因测试夹具把查询对象声明为过宽的 `Wrapper` 接口而在测试编译阶段失败；调整为实际
`LambdaQueryWrapperX` 类型后重跑，最终全量通过。该失败未涉及生产实现。
