# CRM 工单分派与筛选测试

## 自动化结果

运行环境：Ubuntu 26.04 Podman，配置 `podman/config/test-crm-ubuntu-26.04.yaml`。

| 范围 | 结果 |
|---|---|
| CRM 全量回归 | 170/170，通过 170，失败 0，错误 0，跳过 0 |
| 工单分派服务新增场景 | 3/3 通过 |
| `query-all` 显式视图筛选 | 3/3 通过 |
| 分派与视图请求校验 | 3/3 通过 |
| 工单服务专项累计 | 9/9 通过 |

覆盖点包括：创建人分派、全量调度边界、处理人越权、相同处理人拒绝、旧处理人并发条件、
轨迹动作、新处理人通知，以及 `query-all` 用户的我创建/我处理筛选。

## JaCoCo

| 指标 | 覆盖率 |
|---|---:|
| 指令 | 34.60% |
| 分支 | 33.82% |
| 行 | 32.64% |
| 方法 | 22.50% |

原始报告位于 `Server/mitedtsm-module-crm/target/site/jacoco/`，属于构建产物，不提交。

## 真实运行验收

- `W-202607-0003` 创建时处理人为 100，待处理状态下成功分派给 145；
- 详情轨迹包含创建动作和 `actionType=7` 分派动作，新轨迹处理人为 145；
- `crm-work-order-assigned` 通知已写入用户 145；
- 重复分派给 145 返回 `WORK_ORDER_HANDLER_UNCHANGED`；
- 管理员显式“我创建”返回 3 条，“我处理”返回 2 条；
- 类型 2、优先级 3、处理人 145 的组合筛选仅返回该验收工单；
- Server `UP`，Web/Mall 均为 200。
