# 营销活动可维护闭环测试计划

1. 验证新建表单使用真实用户列表并默认当前登录用户，不残留固定用户 ID；
2. 验证草稿、进行中、锁定、终止、完成五态对应操作集合；
3. 验证结束时间必须晚于开始时间；
4. 验证编辑先读取完整服务端对象，避免覆盖既有关联关系；
5. 验证草稿负责人可删除，非草稿和无关用户不可删除；
6. 在 Ubuntu 26.04 容器执行专项、CRM 全量、覆盖率和 Web production build。
