# BPM 自定义审批详情测试结果

| 门禁 | 结果 |
|---|---|
| `verify:bpm-start-navigation` | 8/8 通过，0 失败 |
| 路径与 YAML 契约覆盖率 | 行/分支/函数 100% |
| 相关 Vue、TS、MJS ESLint | 通过，0 warning |
| Ubuntu 26.04 Web 生产构建 | 通过 |
| 三语言包完整性 | 通过 |
| 运行库历史路径修复 | 43 条定义已修复并审计；CRM/OA 受管流程遗留 0 条 |
| 未知历史配置隔离 | `jiandan:1 = 2` 保持原值并被严格拒绝 |

人工回归应分别从待办或已办任务进入回款、合同、报销、退款审批，确认审批详情只显示当前
`businessKey` 对应的单条业务数据，不出现搜索、新增、导出、场景页签和分页。

本功能不维护错误详情路径的前端兼容层。已知历史值通过可审计 SQL 治理；无法可靠判断业务含义的
值明确失败，避免错误页面被静默嵌入审批界面。
