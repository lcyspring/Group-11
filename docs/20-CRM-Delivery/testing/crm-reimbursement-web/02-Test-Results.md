# CRM 报销管理端测试结果

- 日期：2026-07-15
- 配置：`podman/config/verify-crm-reimbursement-web-ubuntu-26.04.yaml`
- 状态：自动化、生产构建与只读运行验收通过

| 检查项 | 结果 |
|---|---|
| Node 专项测试 | 3/3 通过 |
| 目标目录 ESLint | 零告警 |
| 中英阿三语文案 ESLint | 零告警 |
| Ubuntu 26.04 Web 生产构建 | 通过 |
| 全量 `vue-tsc` 报销目录过滤 | 无报销/费用分类类型错误 |
| Web 热替换与首页健康检查 | 通过，`http://127.0.0.1:8081/` 返回 200 |
| 报销页面三个异步 chunk | 均返回 200 |
| 登录用户报销权限 | 查询、创建、修改、删除、费用分类写入 5 项齐全 |
| 报销分页与费用分类接口 | `code=0`，样本 1 条、分类 1 条 |
| 样本详情与动作轨迹 | `BX202607-0001`，2 条明细、1 个附件、2 条轨迹 |
| 服务端总额与金额快照 | 均为 `124.000000` |

审批运行验收未执行。当前 Flowable 没有 `crm-reimbursement-audit` 定义，状态继续由 `CRM-FIN-BUG-009` 跟踪。

全量 `vue-tsc` 本身仍因 Web 其他既有模块的类型债务返回失败，继续由 `WEB-TYPE-BUG-001` 跟踪；本批曾检出的一处费用分类可选 ID 类型错误已修正，复跑后新增目录不再出现在错误清单中。
