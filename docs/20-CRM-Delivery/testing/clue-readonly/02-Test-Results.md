# 已转换线索只读测试结果

执行日期：2026-07-13

## 自动化与构建

| 检查 | 结果 |
|---|---|
| `CrmClueServiceImplTest` | 3/3 |
| `CrmPermissionServiceImplTest` | 2/2 |
| CRM `Crm*Test` 全量 | 42/42，失败 0、错误 0 |
| 相关 Vue 文件 ESLint | 通过 |
| Web `build:prod` | 通过 |
| Server 生产 JAR | 通过 |
| Podman Server/Web/Mall | 健康 |

## 真实 API/MySQL

| 场景 | 实际结果 | 状态 |
|---|---|---|
| 更新字段 | 返回 `1020001002`，名称不变 | 通过 |
| 转移负责人 | 返回 `1020001002`，负责人仍为 1 | 通过 |
| 删除线索 | 返回 `1020001002`，有效线索仍为 1 | 通过 |
| 新增跟进 | 返回 `1020001002`，事务回滚后无新增记录 | 通过 |
| 删除历史跟进 | 返回 `1020001002`，原记录仍有效 | 通过 |
| 添加团队成员 | 返回 `1020001002`，没有新增权限 | 通过 |
| 编辑团队权限 | 返回 `1020001002`，级别不变 | 通过 |
| 移除团队成员 | 返回 `1020001002`，权限仍有效 | 通过 |
| 伪造业务对象 | 用可管理客户 ID 包装线索权限 ID，返回 `1020007000` | 通过 |
| 历史迁移 | 原线索跟进仍可读，客户副本恰好一条 | 通过 |

真实只读验证 10/10。

## 锁等待时序

| 时序 | 实际结果 | 状态 |
|---|---|---|
| 外部锁 → 转换排队 → 更新排队 | 转换成功；更新看到已转换并拒绝；客户保持原字段 | 通过 |
| 外部锁 → 更新排队 → 转换排队 | 更新先成功；转换随后成功；客户名称/手机号为更新后值 | 通过 |

锁序验证 2/2，证明转换使用锁后的当前读而非等待前 RR 快照。

## 清理

最终按 `Codex%` 检查测试线索、客户、联系人、操作日志和关联权限均为 0；测试跟进按业务对象和内容前缀检查为 0。
