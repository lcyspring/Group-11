# CRM 性能与容量基线测试计划

日期：2026-07-17。分支：`develop`。

1. 静态检查脚本语法和单 KDL 参数入口；
2. 使用真实租户账号获取令牌，预热五个只读场景；
3. 每个场景执行 100 次正式采样，并发数为 8；
4. 同时校验 HTTP 200 与业务码 0，统计 p50、p95、p99、错误率和吞吐；
5. 采集 CPU、内存、总表数和 CRM 表数，验证证据可解释；
6. 按显式阈值判定，任一指标越界即整体失败。
