# CRM 公海并发领取测试结果

执行日期：2026-07-14

## 自动化、构建与运行

| 检查 | 结果 |
|---|---|
| Mapper 定向 | 5/5 |
| Customer Service 定向 | 16/16 |
| CRM `Crm*Test` 全量 | 56/56，失败 0，错误 0 |
| Server 生产 JAR | `BUILD SUCCESS` |
| Rootless Podman Server | `/actuator/health` 返回 `UP` |

## 单客户真实并发

临时客户：`740006`；并发用户：1、100。

| 断言 | 实际结果 | 状态 |
|---|---|---|
| API 返回 | 一个 code 0；一个 `1020006001` | 通过 |
| 最终负责人 | 用户 1 | 通过 |
| 有效 Owner 权限 | 仅一条，用户 1 | 通过 |
| 本轮领取历史 | 仅一条，`null -> 1` | 通过 |

## 相反顺序批量并发

临时客户：`740007`、`740008`。

- 用户 1 请求 `[740007,740008]`。
- 用户 100 请求 `[740008,740007]`。
- 实际由用户 100 的批次胜出；另一批次返回 `1020006001`。
- 两个请求均在客户端超时前完成，没有出现 InnoDB 死锁。
- 两位客户最终负责人均为 100，各自只有一条有效 Owner 权限和一条 `TAKE_POOL` 历史。

## 清理

- `740006` 至 `740008` 的客户、CRM 权限和归属历史已物理删除。
- 原缺陷复现记录 `740005` 保留为软删除审计记录，但有效客户数和有效权限数均为 0。
- 名称匹配本轮 `Codex...验证-20260713` 的客户数量为 0。

## 结论

修复满足 `US-CUS-007` 的“并发领取只有一人成功”，同时覆盖相反 ID 顺序的批量并发。前端现有实现可以直接消费后端失败响应，无需补丁。
