# 商城客服运行可靠性测试计划

1. 验证空配置使用当前页面域名生成 `ws://` 或 `wss://` 地址。
2. 验证显式 HTTP、HTTPS、WS、WSS 配置的协议转换与令牌编码。
3. 验证缺少令牌、非法域名和非网络协议被拒绝。
4. 验证组件只显式打开一次 WebSocket。
5. 验证消息列表没有滚动 DOM 非空断言，旧会话异步请求被版本号隔离。
6. 验证组件销毁时取消防抖回调。
7. 在 Ubuntu 26.04 容器中执行测试和 ESLint，并在部署后验证真实 `101` 握手。
