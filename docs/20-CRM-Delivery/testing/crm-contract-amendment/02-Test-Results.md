# 合同补充协议测试结果

执行日期：2026-07-16。

## 自动化结果

| 范围 | 结果 |
|---|---|
| CRM 全量自动化 | 426/426，失败 0、错误 0、跳过 0 |
| 补充协议 Service | 11/11 |
| 合同生命周期 Service | 15/15 |
| 补充协议 Controller | 3/3 |
| 合同生命周期 Controller | 2/2 |
| 补充协议前端专项 ESLint | 通过，警告 0 |
| 本轮前端文件类型诊断过滤 | 0 条新增错误 |
| Ubuntu 26.04 Web production build | 通过 |
| Ubuntu 26.04 Server package/rebuild | 通过，Spring Boot `UP` |

全仓 `vue-tsc` 能在显式 6 GB Node 堆内完整运行，但仍报告既有多模块类型债务。本轮补充协议、
合同生命周期、产品表单、API 和三语资源不在错误清单中。

## 真实 API 验收

租户 1、合同 21 上执行两轮真实流程：

| 验证项 | 结果 |
|---|---|
| 首轮补充协议 | `BC202607-0001`，审批通过，基线 1、目标 2 |
| 次轮补充协议 | ID 2，创建、提交、审批和生效全部通过 |
| 次轮流程实例 | `9eddc771-80d5-11f1-aca5-123dc4e17ad7` |
| 次轮依据附件 | ID 6，审批后不可变 |
| 生命周期 | 创建、提交、审批通过生效动作完整 |
| 跨合同读取 | 拒绝 |
| 跨租户读取 | 拒绝 |
| 最终脚本结果 | `CRM contract amendment acceptance passed` |

数据库迁移 `new-crm-contract-amendment.sql` 随运行清单重复执行成功，流程模型重复治理识别最新版本
并跳过无意义发布。

## 已修复集成问题

- 候选补充协议轨迹曾错误推进当前有效版本，导致创建后提交报“合同版本已经变化”；
- 生命周期响应曾把 `amendmentId` 放在签署对象而非附件对象，导致验收无法识别依据附件归属。

两项均已增加回归测试并通过第二轮真实 API 验收。
