# dasel/KDL 配置覆盖率

本项不属于 Java JaCoCo，采用配置与入口覆盖率记录：

- 正常 KDL 文件：157/157，100%；
- Git 迁移前后配置语义：125/125，100%（排除故意重复键 fixture）；
- 解析负向类别：2/2，100%（重复节点、非法深度）；
- 核心无状态入口：4/4，100%（部署、停止、运行镜像封装、工具链镜像归档）；
- 遗留 Shell YAML 解析/文本替换：0 处。
- `podman/` 项目配置 KDL 文件：0 份；dasel 直接下载官方可执行文件，不产生源码或编译缓存。

完整构建和有状态部署覆盖将在三阶段交付测试目录独立记录。
