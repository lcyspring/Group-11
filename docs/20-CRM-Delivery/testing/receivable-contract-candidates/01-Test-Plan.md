# 可回款合同候选测试计划

日期：2026-07-16。分支：`develop`。

## 目标

1. 验证候选只包含审批通过合同；
2. 验证普通用户直接 OWNER/WRITE 与管理员全量分支；
3. 验证剩余额度扣除审批中和已审批回款；
4. 验证合同优先选择、客户自动带出、客户过滤和无额度禁用；
5. 使用显式 KDL 在 Ubuntu 26.04 容器完成自动化、覆盖率和生产构建；
6. 使用显式账号 KDL 完成真实 API 与 MySQL 对账。
