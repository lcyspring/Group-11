# 工单统计趋势图测试计划

1. 以空趋势数组构造初始 ECharts options；
2. 以两天创建/完结数据构造加载后 options；
3. 断言两次构造返回不同引用，初始横轴不被后续构造污染；
4. 断言日期、创建数、完结数分别进入正确轴和序列；
5. 运行 ESLint、Ubuntu 26.04 生产构建和语言包完整性校验；
6. 封装 Web 运行镜像并通过 `replace-web` 健康检查。
