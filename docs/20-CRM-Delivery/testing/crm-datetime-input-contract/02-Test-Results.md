# CRM 日期入参契约测试结果

- 日期：2026-07-15
- 环境：Ubuntu 26.04 rootless Podman
- 分支：`develop`
- 状态：通过

| 检查项 | 结果 |
|---|---|
| 专项单测 | 4 通过，0 失败，0 跳过 |
| 整数时间戳 | 通过 |
| 数字字符串 | 通过，与 Web `value-format="x"` 一致 |
| 格式化日期文本 | 拒绝，业务码 `400` |
| 非字符串/整数 JSON 类型 | 拒绝 |
| Server 生产构建 | Ubuntu 26.04，37 个 reactor 模块成功 |
| CRM 全量回归 | 264 通过，0 失败，0 错误，0 跳过 |
| 8080 热替换 | 成功，健康状态 `UP` |
| 毫秒字符串真实接口 | 正确解析后进入“已审批禁止编辑”业务校验 |
| 回款样本 | `HK20260715000001`，`2026-07-15 14:00:00`，状态 20 |
| 合同累计回款 | `50.000000` |
| 2026-07 回款业绩 | `50.000000` |
| 2026-07 回款排行榜 | 用户 1，`50.000000` |
| 非法日期探针落库 | 0 行 |
| 四类 CRM 遗留运行实例/待办 | 0 / 0 |

服务统一使用 HTTP 200 封装 `CommonResult`，因此非法参数的 HTTP 状态仍为 200，但响应
业务码为 `400`；这是项目全局响应契约，不将其记为本功能失败。
