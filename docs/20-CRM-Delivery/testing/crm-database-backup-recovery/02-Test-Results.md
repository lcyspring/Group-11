# CRM 数据备份恢复测试结果

日期：2026-07-17。分支：`develop`。

| 场景 | 结果 |
|---|---|
| check 模式 | 通过，未写归档、未改数据库 |
| 运行库一致性备份 | 通过，gzip 约 1.6 MiB |
| SHA-256 与 gzip | 正常归档通过；伪造 checksum 明确拒绝 |
| 隔离库恢复 | 通过，恢复 369 张表 |
| CRM 核心表 | 7/7 |
| 演练清理 | `mitedtsm_restore_verify` 自动删除，真源库仍 369 张表 |
| 真源保护 | 未二次授权拒绝；授权后 Server 仍运行也拒绝 |

演练后真源库存在，当前有效客户 6、合同 10；隔离库不存在。归档位于 `/tmp`，不进入 Git。
