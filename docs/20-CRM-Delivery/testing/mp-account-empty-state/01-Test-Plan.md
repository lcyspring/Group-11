# 公众号账号空状态测试计划

- 验证模块专用三语键解析；
- 验证空账号时保留当前公众号子页面，不产生 Toast 或自动跳转；
- 验证只有用户主动点击“配置公众号”才进入账号管理；
- 验证初始化异常显示可重试空状态且不会形成未处理 Promise；
- 验证 `v-model` 初始选择与账号变更事件一致；
- 验证账号为空或加载失败时，全部十个消费页面释放加载态并清空旧数据；
- 在 Ubuntu 26.04 容器执行专项、ESLint 和 Web production build。
