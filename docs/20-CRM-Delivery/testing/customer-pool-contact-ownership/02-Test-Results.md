# 客户公海联系人归属测试结果

- 日期：2026-07-15
- 环境：Ubuntu 26.04 rootless Podman
- 状态：通过

| 检查项 | 结果 |
|---|---|
| 入池联系人同步 | 通过，新负责人为 `null` |
| 领取/分配联系人同步 | 通过，每个联系人同步新负责人 |
| 旧 OWNER 删除 | 通过 |
| 新 OWNER 新建/升级 | 通过 |
| READ/WRITE 团队权限保留 | 通过 |
| 入池操作顺序 | 先联系人，后客户 OWNER 权限 |
| 重复 OWNER 收敛 | 通过，同一负责人只保留一个 OWNER |
| CRM 全量回归 | 268 通过，0 失败，0 错误，0 跳过 |
| Server 生产构建 | Ubuntu 26.04，37 个 reactor 模块成功 |
| Server 热替换 | 通过，8080 健康状态 `UP` |
| Web / Mall | 8081 / 8082 保持可用 |
| 迁移前基线 | 负责人不一致 1、失效 OWNER 4、缺失 OWNER 1、重复组 0 |
| 最终迁移结果 | 上述四类异常全部为 0 |
| 最终迁移幂等 | 连续两次结果与权限指纹完全一致 |
| 客户 OWNER 指纹 | 迁移前后均为 `10 / 466 / 112`（数量 / id 合计 / user_id 合计） |
| 联系人 OWNER 指纹 | 两次最终迁移均为 `5 / 456 / 218` |
| 孤立联系人 | 联系人 13 仍为 `customer_id=null`、负责人 112、原 OWNER 1 条 |

首轮 267 项回归的唯一错误是新测试代理将 MyBatis `Collection` 强转为 `Set`；
修正夹具为接受任意 `Collection` 后，原样全量复跑通过。该错误未进入产品代码。

迁移评审还发现业务类型常量错误；修复和中间运行结果记录在
`CRM-CUSTOMER-POOL-BUG-002`。最终脚本使用单一会话常量，并通过显式 KDL 连续
执行两次。Ubuntu 26.04 完整 Server 构建和最新 JAR 热替换均已通过。
