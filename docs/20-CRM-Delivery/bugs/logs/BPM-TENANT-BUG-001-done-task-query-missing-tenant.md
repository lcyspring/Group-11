# BPM-TENANT-BUG-001：已办任务查询遗漏租户条件

日期：2026-07-14

分支：`develop`

状态：已修复

## 现象与影响

待办分页已有 `taskTenantId`，已办分页只按审批人查询。多租户使用相同用户数字编号时，历史
任务存在串租户返回风险，CRM 统一待办也无法建立一致的待办/已办边界。

## 修复与验证

- 已办分页及历史任务的 ID、集合、流程实例查询统一增加当前租户；
- 运行任务按 ID、集合和流程实例查询同步收口；
- Mockito 负向验证确认不走“无租户”查询；BPM 54 个测试通过且失败为 0。
