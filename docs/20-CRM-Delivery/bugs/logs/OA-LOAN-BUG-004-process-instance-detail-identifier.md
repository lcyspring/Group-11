# OA 借款流程详情使用错误编号类型

## 现象

从“我的流程”打开借款审批详情时，路由携带的是 BPM 流程实例 UUID；借款详情页将其强制转换为 Long，最终显示“借款编号无效”。

## 修复

- 新增按 `processInstanceId` 查询借款业务单据的接口；
- 借款列表仍使用显式 `loanId`；
- BPM 流程入口自动使用流程实例 UUID 反查业务借款，再加载还款记录；
- 无参数时仍保留明确的无效编号提示。

## 验证

专项契约测试覆盖借款 ID 与流程实例 ID 两种详情入口。
