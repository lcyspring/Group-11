# OA-LOAN-BUG-003 借款列表返回后未自动刷新

## 现象

新增借款、审批或登记还款后返回借款管理，列表仍显示旧状态或旧未还金额，必须手动刷新。

## 根因

页签缓存复用列表组件，原页面只在首次 `onMounted` 查询，返回页面不会再次加载。

## 修复

首次加载完成后记录初始化状态，页面重新 `onActivated` 时重新请求列表；保留当前筛选和分页，不依赖整页刷新。
