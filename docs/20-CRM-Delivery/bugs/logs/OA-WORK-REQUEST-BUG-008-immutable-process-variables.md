# OA 请示发起返回服务器错误

## 现象

发起请示写入业务单据后，创建 Flowable 实例时报 `UnsupportedOperationException`，事务回滚并返回服务器错误。

## 根因

请示服务使用 `Map.of` 创建不可变流程变量；BPM 框架会过滤内部表单变量并调用 `remove`，不可变 Map 无法修改。

## 修复

流程变量改用可变 `HashMap`，保持与借款、出差等既有审批服务一致；通过真实接口验证业务单据、流程实例及状态回写链路。
