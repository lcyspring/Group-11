# BPM-任务撤回失败提示

## 现象

已办任务列表对流程已经结束的记录仍显示“撤回”，点击后返回“撤回失败，流程实例未运行”。

## 修复

已办任务列表仅对后端明确判定为“运行中且流程定义允许撤回”的记录显示撤回操作；已结束流程保留审批历史查看，避免用户触发必然失败的请求。后端同时返回 `withdrawable` 字段，避免历史实例摘要被误判为可撤回。

## 验证

- 已结束流程：不显示撤回按钮，可查看审批历史。
- 仍运行流程：保留撤回按钮，继续由后端校验模型权限及后续节点状态。
