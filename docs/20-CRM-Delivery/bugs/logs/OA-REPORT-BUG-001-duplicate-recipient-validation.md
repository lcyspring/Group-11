# OA-REPORT-BUG-001 工作报告接收人重复校验

## 现象

工作报告创建时，同一组接收人会调用两次用户有效性校验。结果不受影响，但会产生重复的用户服务查询。

## 根因

创建入口先显式调用接收人校验，随后接收人规范化方法再次执行相同校验，职责发生重复。

## 修复

- 统一由接收人规范化方法完成去空、去重、排除作者、非空检查和用户有效性校验；
- 创建和更新入口只调用一次规范化方法；
- 单元测试固定每次业务操作只进行一次用户批量校验。

## 验证

专项门禁确认最终接收人不包含作者和重复项，同时 `validateUserList` 只调用一次。
