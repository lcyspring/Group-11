# CRM-CORE-BUG-017：商机阶段与结束状态可同时提交

更新日期：2026-07-14

## 现象

`isStatusValid` 只要求 `statusId` 或 `endStatus` 至少一个非空，因此两者同时提交
也会通过，产生“推进阶段同时结束”的歧义状态。

## 根因

Bean Validation 使用逻辑 OR，没有表达状态迁移命令必须二选一。

## 修复关键

校验改为 XOR：活动阶段更新与结束状态更新必须且只能选择一个。

## 验证

新增同时提交的负向用例；请求约束 6/6、Ubuntu CRM 76/76。

## 状态

已关闭。
