# CRM-CONTRACT-BUG-009：签署作废网络重试不幂等

发现日期：2026-07-14。状态：Fixed。

签署作废初版在记录已是作废状态时统一返回“状态不允许”，导致客户端在首次提交成功但响应丢失后
无法安全重试，也偏离 Provider 稳定请求号的设计。

修复后，同一合同、同一作废原因的重复命令直接成功，不再次调用 Provider、不更新签署记录、也不
重复写轨迹；不同原因仍返回状态冲突，禁止覆盖原作废事实。专项 Mockito 测试验证三个零副作用断言。
