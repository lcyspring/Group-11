# CRM-RECEIVABLE-OVERDUE-BUG-001 逾期提醒仅靠查询判定

## 现象

回款计划页面可以筛选逾期记录，但系统没有定时检测、提醒发送记录或失败重试，用户必须主动打开页面跟进。

## 根因

原实现只在查询时根据计划日期和回款审批状态计算逾期，缺少持久化提醒事实和幂等边界。

## 修复

- 增加按租户、计划和业务日期唯一的逾期提醒事实表；
- 增加 YAML 驱动的定时扫描、分布式锁、批量上限和重试策略；
- 使用 System Notify 发送站内信，记录成功、失败、尝试次数和最近错误；
- 审批通过或计划不再逾期时关闭待发送事实，避免陈旧通知。
