# BPM-IAM-BUG-001：模型列表和排序接口缺少显式权限

日期：2026-07-14

分支：`develop`

状态：已修复

## 现象与影响

模型详情和普通更新已有权限注解，但模型列表与批量排序遗漏。仅依赖菜单隐藏不能保护直接
HTTP 请求，排序接口尤其可能被无更新权限用户调用。

## 修复与验证

- 模型列表增加 `bpm:model:query`；
- 批量排序增加 `bpm:model:update`；
- Server 在 Ubuntu 26.04 中重新编译并完成热替换。
