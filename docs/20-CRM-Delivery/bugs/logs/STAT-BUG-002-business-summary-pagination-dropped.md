# STAT-BUG-002：商机汇总分页参数被父级筛选覆盖

发现日期：2026-07-13。关闭日期：2026-07-16。级别：P1。

商机汇总列表曾只提交父级统计条件，分页组件的 `pageNo/pageSize` 没有进入请求，翻页后仍读取首
页数据。修复后请求先展开父级筛选，再显式写入本地分页参数，保证本地分页具有最终优先级。

最终契约测试锁定 `getBusinessPageByDate` 的参数合并顺序和两个分页字段。Ubuntu 26.04 统计前端
10/10、ESLint 零警告、生产构建通过。
