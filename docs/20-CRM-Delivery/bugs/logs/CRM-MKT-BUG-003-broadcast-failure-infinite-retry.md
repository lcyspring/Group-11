# CRM-MKT-BUG-003：群发失败收件人导致同步发送循环

状态：已修复
分支：`develop`

- 现象：短信或邮件提供商返回失败后，同一次发送请求可能持续重新选择该失败收件人，无法结束。
- 根因：待发送查询同时选择 `PENDING` 和 `FAILED`，失败记录写回后会立刻再次进入下一批。
- 修复：正常发送只领取 `PENDING`，领取时原子切换到 `SENDING`；失败记录仅在显式“重试失败”操作中重置为 `PENDING`。
- 回归：覆盖多批发送、失败终止、显式重试和联系人/客户同意优先级。
