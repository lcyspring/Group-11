# STAT-BUG-003：业绩年份与时间戳范围共用模型

发现日期：2026-07-13。关闭日期：2026-07-16。级别：P1。

年份选择器曾与完整起止时间数组混用，导致组件值类型和 API 时间范围互相覆盖。修复后
`selectedYear` 只保存四位年份，查询时显式生成当年 1 月 1 日至 12 月 31 日的时间范围。

最终契约测试验证模板只绑定 `selectedYear`、年份 ref 独立存在、两个时间端点由所选年份生成，且
不存在对 `queryParams.times` 的直接表单绑定。Ubuntu 26.04 统计前端 10/10、生产构建通过。
