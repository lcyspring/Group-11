# CRM-MKT-FE-BUG-004：群发收件人状态编号与后端契约错位

## 现象

群发收件人结果能够返回，但前端会把已记录、已发送、失败和抑制状态显示成错误标签。

## 根因

前端状态常量使用了连续十位编号 `10/20/30/40/50/60`，后端真实枚举为
`10/15/20/30/40/50`。早期页面直接显示数字，因此该契约漂移未被发现。

## 修复

- 前端状态常量严格对齐后端枚举；
- 增加枚举值契约测试；
- 真实 API/MySQL 验收断言 `record-only` 模式最终状态为 `RECORDED(50)`。

## 分支

`develop`
