# OA 日程视图状态不完整

## 现象

日程页面加入日、周、月视图入口后，切换状态和查询时间范围没有完整接入脚本，且编辑时间与日历查询范围共用同一变量，可能导致运行时未定义或保存错误时间。

## 修复

- 日、周、月视图根据锚点日期计算独立查询范围；
- 支持上一周期、下一周期和返回今天；
- 日程编辑时间使用独立区间，不再复用当前日历视图范围；
- 编辑现有日程时同步回填实际开始、结束时间。

## 验证

专项契约测试覆盖三种视图范围、周期导航和编辑区间隔离。
