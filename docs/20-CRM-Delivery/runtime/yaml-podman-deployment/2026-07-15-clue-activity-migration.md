# 2026-07-15 线索活动迁移运行验收

- 分支：`develop`
- 构建：Ubuntu 26.04 容器
- 运行：rootless Podman，显式 YAML 配置

完整 `runtime-local.yaml` 部署重新打包 Server 和 Web，重复执行
`new-crm-clue-activity-migration.sql` 后正常启动。

| 服务 | 结果 |
|---|---|
| Server | `http://127.0.0.1:8080/actuator/health` 返回 `UP` |
| Web | `http://127.0.0.1:8081/` 返回 200，包含最新活动详情页 |
| Mall | `http://127.0.0.1:8082/` 返回 200 |

真实验收创建线索 413、客户 740016、首联系人 74、两个任务、一条通话和一条短信。任务状态
轨迹为创建、开始、完成；转换审计返回任务 2、通话 1、短信 1，客户活动均保留来源线索，原
线索不再返回活动，重复转换被拒绝。脚本退出时清理临时数据，五类残留计数均为 0。

部署后 Server 日志没有 ERROR、构建失败或死锁。重复转换负向用例产生的业务 WARN 属于预期
拒绝，并非运行异常。
