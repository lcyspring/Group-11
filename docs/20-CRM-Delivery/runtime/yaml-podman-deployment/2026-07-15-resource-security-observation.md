# 2026-07-15 CRM 资源安全观察环境

## 构建与服务

- 分支：`develop`；
- 构建：Ubuntu 26.04 容器，CRM 231/231、Infra 文件专项 28/28；
- 运行：rootless Podman，配置 `podman/config/runtime-local.yaml`；
- Server `UP`，Web/Mall HTTP 200；服务在验收后保持运行。

## 合同附件真实链路

在合同 7 上传文本附件，数据库路径为 `crm-protected/contract/7/20260715/...`。生命周期响应不包含
`fileUrl`，认证下载内容与源文件一致；公共 Infra URL 返回 404。把该临时 URL 改绑合同 11 返回
`1020000024`，证明合同专属目录约束生效。未登录调用业务下载返回业务码 401“账号未登录”。

验收附件和受管文件均通过 API 删除，有效残留为 0。

## 导出与历史消息

- 管理员客户导出 HTTP 200，文件 4572 字节；
- 临时为普通用户配置客户导出动作和单一客户 READ 权限，导出返回 `1020007010`；
- 临时角色菜单、对象权限和测试用户密码哈希全部恢复，临时权限行 0；
- 历史工单站内信 ID 11～15 正确，双重编码特征计数 0。

## 显式遗留风险

新附件已经关闭匿名公共读取。现有一条历史附件仍引用旧公共目录，已保存 URL 实测仍为 200；要关闭该
链接必须读取原文件、写入合同专属保护目录、更新引用并处理旧存储对象，不能用数据库路径硬改冒充迁移。
