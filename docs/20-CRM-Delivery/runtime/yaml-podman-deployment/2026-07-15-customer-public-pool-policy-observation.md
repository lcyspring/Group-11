# 2026-07-15 客户公海规则运行观察

Ubuntu 26.04 容器完成 Server 37 模块、CRM 292/292 和 Web 生产构建。数据库迁移通过
显式运行 YAML 重复执行，Server 与 Web 随后完整替换到 rootless Podman。

租户 1 的有效规则为未跟进 31 天、未成交 5 天；每日领取 10、重复领取冷却 30 天、
高价值阈值 4、保护倍数 2、商机/合同保护开启、单批 500、YAML 安全上限 5000。前两项来自租户历史覆盖，
其余缺省值来自 `mitedtsm.crm.pool-policy`，证明配置合并而非 Java 硬编码。

真实配置接口返回 `autoPoolBatchSize=500`、`autoPoolMaxBatchSize=5000`；尝试保存 5001 返回
`1020006028`，再次查询仍为 500，证明服务端拒绝超限值且没有产生部分写入。

隔离运行样本完成金牌客户创建、手工入池、公海列表元数据、自助领取、再次入池、冷却拒绝、
主管分配和删除。第二次自助领取返回 `1020006025`；所有临时业务记录、权限、计数和通知
物理清理为 0。最终 Server 健康为 UP，Web 与 Mall 均返回 HTTP 200，继续保持运行供观察。
