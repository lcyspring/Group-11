# CRM 回款审批与统计观察

- 日期：2026-07-15
- 分支：`develop`
- 运行方式：Ubuntu 26.04 构建 + rootless Podman

| 服务 | 观察结果 |
|---|---|
| Server | `http://127.0.0.1:8080/actuator/health`，`UP` |
| Web | `http://127.0.0.1:8081/`，200 |
| Mall | `http://127.0.0.1:8082/`，200 |

回款 `HK20260715000001` 关联已通过合同 `HT20260714000001`，完成草稿、提交、
`crm-receivable-audit` 待办和审批通过，状态为 `0 -> 10 -> 20`。审批后无遗留
待办，合同累计回款、客户 360 明细、2026-07 回款业绩与排行榜均为
`50.000000`。

日期契约修复通过 `runtime-local-rebuild-server.yaml` 热替换，未重建 Web/Mall，三个观察
入口保持可用。
