# 2026-07-15 CRM 报销后端运行验收

- 分支：`develop`
- 运行：Ubuntu 26.04 构建产物 + rootless Podman
- 配置：显式 KDL，命令行仅传配置路径

## 结果

| 检查项 | 结果 |
|---|---|
| Server | `http://127.0.0.1:8080/actuator/health`，UP |
| Web | `http://127.0.0.1:8081/`，保持在线 |
| Mall | `http://127.0.0.1:8082/`，保持在线 |
| 四张报销表 | 已创建 |
| 迁移幂等 | 在已有分类和报销数据后第二次执行成功 |
| 费用分类 | `TEST_TRAVEL` 创建成功 |
| 报销草稿 | `BX202607-0001` 创建并读取成功 |
| 金额真源 | 两条明细合计 124，主单返回 `124.000000 CNY` |
| 受保护附件 | 专用入口上传、更新后详情返回 1 个附件 |
| 显式并发控制 | 真实更新版本从 0 原子递增为 1 |
| 审批定义 | 未部署，记录为 `CRM-FIN-BUG-009` |

服务继续保持运行，供人工观察。
