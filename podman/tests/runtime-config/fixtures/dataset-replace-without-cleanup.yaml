schema_version: 1

operation:
  action: check

container:
  mysql: mitedtsm-rootless-mysql

mysql:
  database: mitedtsm_database
  username: root
  password: check-only
  dataset: legacy-demo-v1
  dataset_mode: replace
